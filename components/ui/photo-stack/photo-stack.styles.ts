import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row'
  },

  photo: {
    width: 24,
    height: 24,

    borderRadius: 12,

    marginLeft: -8
  },

  photo__first: {
    marginLeft: 0
  }
});
